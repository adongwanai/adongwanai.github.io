---
import '@fontsource/inter';
import '@fontsource/inter/500.css';
import '@fontsource/inter/600.css';
import '@fontsource/inter/700.css';
import '@fontsource/jetbrains-mono';
import ThemeToggle from '../components/ThemeToggle.astro';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = '阿东玩AI - Vibe Coding',
  description = '大模型算法工程师 | AI Agent 开发者 | Vibe Coding 实践者'
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);

const getPath = (path: string) => {
  const base = import.meta.env.BASE_URL;
  return base === '/' ? path : `${base.replace(/\/$/, '')}${path}`;
};
---

<!doctype html>
<html lang="zh-CN" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href={getPath('/favicon.svg')} />
    <link rel="canonical" href={canonicalURL} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="https://adongwanai.github.io/og-image.jpg" />
    <meta property="og:locale" content="zh_CN" />
    <meta property="og:site_name" content="阿东玩AI" />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content="https://adongwanai.github.io/og-image.jpg" />

    <!-- Additional SEO -->
    <meta name="keywords" content="大模型算法工程师, AI Agent 开发, Vibe Coding, Claude Skills, MCP 服务器, AI 工作流, LLM, Astro" />
    <meta name="author" content="阿东玩AI" />
    <meta name="theme-color" content="#3b82f6" />
    <script is:inline>
      // Prevent Flash of Unstyled Content
      // Set the dark class based on local storage or system preference
      if (localStorage.getItem('color-theme') === 'dark' || (!('color-theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark');
      } else {
        document.documentElement.classList.remove('dark');
      }
    </script>
  </head>
  <body class="bg-gray-50 dark:bg-primary text-gray-900 dark:text-gray-100 min-h-screen flex flex-col font-sans selection:bg-accent selection:text-white transition-colors duration-300">
    <header class="fixed top-0 left-0 right-0 bg-white/80 dark:bg-primary/80 backdrop-blur-md border-b border-gray-200 dark:border-white/10 z-50 transition-colors duration-300">
      <nav class="max-w-7xl mx-auto px-6 h-[70px] flex justify-between items-center">
        <a href={getPath('/')} class="text-xl font-bold">
          <span class="bg-gradient-to-br from-blue-500 to-violet-600 bg-clip-text text-transparent">阿东玩AI</span>
        </a>
        <div class="flex items-center gap-4">
          <!-- Desktop Navigation -->
          <ul class="hidden lg:flex gap-8 items-center">
            <li><a href={getPath('/#projects')} class={`transition-colors duration-300 text-sm font-medium ${Astro.url.pathname === '/#projects' ? 'text-accent font-bold' : 'text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white'}`}>开源项目</a></li>
            <li><a href={getPath('/vibecoding')} class={`transition-colors duration-300 text-sm font-medium ${Astro.url.pathname.startsWith('/vibecoding') ? 'text-accent font-bold' : 'text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white'}`}>📚 Vibe Coding 专栏库</a></li>
            <li><a href={getPath('/#about')} class={`transition-colors duration-300 text-sm font-medium ${Astro.url.pathname === '/#about' ? 'text-accent font-bold' : 'text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white'}`}>关于我</a></li>
            <li><a href="https://github.com/adongwanai" target="_blank" class="text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white transition-colors duration-300 text-sm font-medium flex items-center gap-1">GitHub<svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg></a></li>
            <li>
              <a href={getPath('/skills')} class={`px-4 py-2 rounded-full text-sm font-semibold transition-all duration-300 border flex items-center gap-2 shadow-[0_0_15px_rgba(59,130,246,0.1)] dark:shadow-[0_0_15px_rgba(59,130,246,0.3)] hover:shadow-[0_0_25px_rgba(59,130,246,0.2)] dark:hover:shadow-[0_0_25px_rgba(59,130,246,0.5)] ${Astro.url.pathname.startsWith('/skills') ? 'bg-blue-600 text-white border-blue-600 dark:bg-blue-500 dark:border-blue-500' : 'bg-blue-50 dark:bg-accent/10 text-accent hover:bg-blue-100 dark:hover:bg-accent/20 border-blue-200 dark:border-accent/20'}`}>
                <span>✨</span> Agent Skills 库
              </a>
            </li>
            <li>
              <a href="https://xyzq.xyz" target="_blank" class="px-5 py-2 rounded-full text-sm font-bold text-white bg-gradient-to-r from-violet-600 to-fuchsia-600 hover:from-violet-500 hover:to-fuchsia-500 shadow-lg shadow-violet-500/30 hover:shadow-violet-500/50 transition-all duration-300 flex items-center gap-2 hover:-translate-y-0.5">
                💎 加入实战社区
              </a>
            </li>
          </ul>
          <div class="flex items-center gap-2">
            <ThemeToggle />
            <!-- Mobile Menu Button -->
            <button
              id="mobile-menu-btn"
              class="lg:hidden p-2 min-h-[44px] min-w-[44px] rounded-lg hover:bg-gray-100 dark:hover:bg-white/10 transition-colors duration-300"
              aria-label="Toggle menu"
            >
              <svg id="menu-icon" class="w-6 h-6 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path class="menu-open" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                <path class="menu-close hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
              </svg>
            </button>
          </div>
        </div>
      </nav>

      <!-- Mobile Menu Overlay -->
      <div
        id="mobile-menu-overlay"
        class="lg:hidden fixed inset-0 bg-black/50 backdrop-blur-sm z-40 opacity-0 pointer-events-none transition-opacity duration-300"
      ></div>

      <!-- Mobile Menu Drawer -->
      <div
        id="mobile-menu"
        class="lg:hidden fixed top-0 right-0 h-full w-72 max-w-[80vw] bg-white dark:bg-primary shadow-2xl z-50 transform translate-x-full transition-transform duration-300 ease-out"
      >
        <div class="p-6">
          <div class="flex justify-between items-center mb-8">
            <span class="text-lg font-bold bg-gradient-to-br from-blue-500 to-violet-600 bg-clip-text text-transparent">菜单</span>
            <button
              id="mobile-menu-close"
              class="p-2 min-h-[44px] min-w-[44px] rounded-lg hover:bg-gray-100 dark:hover:bg-white/10 transition-colors duration-300"
              aria-label="Close menu"
            >
              <svg class="w-6 h-6 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
              </svg>
            </button>
          </div>

          <nav class="flex flex-col gap-4">
            <a href={getPath('/#projects')} class="mobile-nav-link text-gray-700 dark:text-gray-300 hover:text-blue-500 dark:hover:text-blue-400 py-3 px-4 rounded-lg hover:bg-gray-50 dark:hover:bg-white/5 transition-all duration-300 font-medium">开源项目</a>
            <a href={getPath('/vibecoding')} class={`mobile-nav-link py-3 px-4 rounded-lg transition-all duration-300 font-medium ${Astro.url.pathname.startsWith('/vibecoding') ? 'text-blue-600 dark:text-blue-400 bg-blue-50 dark:bg-blue-500/10' : 'text-gray-700 dark:text-gray-300 hover:text-blue-500 dark:hover:text-blue-400 hover:bg-gray-50 dark:hover:bg-white/5'}`}>📚 Vibe Coding 专栏库</a>
            <a href={getPath('/#about')} class="mobile-nav-link text-gray-700 dark:text-gray-300 hover:text-blue-500 dark:hover:text-blue-400 py-3 px-4 rounded-lg hover:bg-gray-50 dark:hover:bg-white/5 transition-all duration-300 font-medium">关于我</a>
            <a href="https://github.com/adongwanai" target="_blank" class="mobile-nav-link text-gray-700 dark:text-gray-300 hover:text-blue-500 dark:hover:text-blue-400 py-3 px-4 rounded-lg hover:bg-gray-50 dark:hover:bg-white/5 transition-all duration-300 font-medium flex items-center gap-2">GitHub <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg></a>
            <a href={getPath('/skills')} class={`mobile-nav-link py-3 px-4 rounded-lg font-semibold transition-all duration-300 ${Astro.url.pathname.startsWith('/skills') ? 'bg-blue-600 text-white dark:bg-blue-500' : 'text-accent bg-blue-50 dark:bg-accent/10 hover:bg-blue-100 dark:hover:bg-accent/20'}`}>✨ Agent Skills 库</a>
            <a href="https://xyzq.xyz" target="_blank" class="mobile-nav-link py-3 px-4 rounded-lg font-bold text-white bg-gradient-to-r from-violet-600 to-fuchsia-600 hover:from-violet-500 hover:to-fuchsia-500 transition-all duration-300 shadow-md flex items-center gap-2">💎 加入实战社区</a>
          </nav>
        </div>
      </div>
    </header>

    <!-- Reading Progress Bar -->
    <div id="reading-progress" class="fixed top-[70px] left-0 h-1 bg-gradient-to-r from-blue-500 to-violet-500 z-50 transition-all duration-150 pointer-events-none" style="width: 0%"></div>

    <main class="flex-1 pt-[70px]">
      <slot />
    </main>

    <footer class="bg-gray-100 dark:bg-secondary border-t border-gray-200 dark:border-white/10 py-8 mt-auto transition-colors duration-300">
      <div class="max-w-7xl mx-auto px-6 flex flex-col md:flex-row justify-between items-center gap-4">
        <p class="text-gray-500 text-sm">&copy; {new Date().getFullYear()} 阿东玩AI. 用 Astro & Vibe Coding 构建</p>
        <div class="flex gap-6">
          <a href="https://github.com/adongwanai" target="_blank" rel="noopener" class="text-gray-500 dark:text-gray-400 hover:text-accent transition-colors text-sm">GitHub</a>
          <a href="https://www.xiaohongshu.com/user/profile/5f310fd50000000001009df5" target="_blank" rel="noopener" class="text-gray-500 dark:text-gray-400 hover:text-accent transition-colors text-sm">小红书</a>
        </div>
      </div>
    </footer>

    <!-- Back to Top Button -->
    <button
      id="back-to-top"
      class="fixed bottom-8 right-8 w-12 h-12 bg-blue-600 hover:bg-blue-700 text-white rounded-full shadow-lg opacity-0 pointer-events-none transition-all duration-300 z-50 flex items-center justify-center"
      aria-label="回到顶部"
    >
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 10l7-7m0 0l7 7m-7-7v18"/>
      </svg>
    </button>

    <script>
      // Reading Progress and Back to Top Header Logic
      const progressContainer = document.getElementById('reading-progress');
      const backToTopBtn = document.getElementById('back-to-top');

      window.addEventListener('scroll', () => {
        // Progress Bar
        const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
        const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
        const scrolled = height > 0 ? (winScroll / height) * 100 : 0;
        if (progressContainer) progressContainer.style.width = scrolled + '%';

        // Back to Top Button
        if (winScroll > 500) {
          backToTopBtn?.classList.remove('opacity-0', 'pointer-events-none');
          backToTopBtn?.classList.add('opacity-100', 'pointer-events-auto');
        } else {
          backToTopBtn?.classList.remove('opacity-100', 'pointer-events-auto');
          backToTopBtn?.classList.add('opacity-0', 'pointer-events-none');
        }
      });

      backToTopBtn?.addEventListener('click', () => {
        window.scrollTo({ top: 0, behavior: 'smooth' });
      });

      // Mobile menu functionality
      const mobileMenuBtn = document.getElementById('mobile-menu-btn');
      const mobileMenuClose = document.getElementById('mobile-menu-close');
      const mobileMenu = document.getElementById('mobile-menu');
      const mobileMenuOverlay = document.getElementById('mobile-menu-overlay');
      const menuIcon = document.getElementById('menu-icon');
      const menuOpenIcon = menuIcon?.querySelector('.menu-open');
      const menuCloseIcon = menuIcon?.querySelector('.menu-close');

      function openMenu() {
        mobileMenu?.classList.remove('translate-x-full');
        mobileMenuOverlay?.classList.remove('opacity-0', 'pointer-events-none');
        mobileMenuOverlay?.classList.add('opacity-100', 'pointer-events-auto');
        document.body.style.overflow = 'hidden';
        menuOpenIcon?.classList.add('hidden');
        menuCloseIcon?.classList.remove('hidden');
      }

      function closeMenu() {
        mobileMenu?.classList.add('translate-x-full');
        mobileMenuOverlay?.classList.remove('opacity-100', 'pointer-events-auto');
        mobileMenuOverlay?.classList.add('opacity-0', 'pointer-events-none');
        document.body.style.overflow = '';
        menuOpenIcon?.classList.remove('hidden');
        menuCloseIcon?.classList.add('hidden');
      }

      mobileMenuBtn?.addEventListener('click', openMenu);
      mobileMenuClose?.addEventListener('click', closeMenu);
      mobileMenuOverlay?.addEventListener('click', closeMenu);

      // Close menu when clicking nav links
      document.querySelectorAll('.mobile-nav-link').forEach(link => {
        link.addEventListener('click', closeMenu);
      });

      // Close menu on escape key
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') closeMenu();
      });
    </script>
  </body>
</html>
