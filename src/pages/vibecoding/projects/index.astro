---
import Layout from '../../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const getPath = (path: string) => {
  const base = import.meta.env.BASE_URL;
  return base === '/' ? path : `${base.replace(/\/$/, '')}${path}`;
};

const allProjects = await getCollection('vibecoding');
const projectContent = allProjects
  .filter(item => item.data.category === 'project')
  .sort((a, b) => a.data.order - b.data.order);

// Split free and premium
const freeProjects = projectContent.filter(p => p.data.tier === 'free');
const premiumProjects = projectContent.filter(p => p.data.tier === 'premium');

// Group premium projects by subdirectory
const groupedPremium = premiumProjects.reduce((acc, item) => {
  const parts = item.slug.split('/');
  const folder = parts.length > 2 ? parts[1] : 'overview'; // projects/{folder}/slug
  if (!acc[folder]) acc[folder] = [];
  acc[folder].push(item);
  return acc;
}, {} as Record<string, typeof premiumProjects>);

const folderNames: Record<string, string> = {
  'overview': '📖 概述',
  'career': '💼 Vibe找工作',
  'media-automation': '📱 Vibe自媒体',
  'agents': '🤖 Agent 开发',
  'fullstack': '🌐 Fullstack',
  'research': '🔬 Vibe科研',
  'tools-dev': '🛠️ Vibe工具开发',
  'business': '💰 Vibe商业变现',
};

const memberCount = 1000; // TODO: 动态获取
---

<Layout title="Vibe Coding 项目教程 - 实战学习 | 阿东玩AI">
  <div class="pt-[90px] pb-24 px-6 max-w-7xl mx-auto">
    <!-- Hero -->
    <div class="text-center mb-16">
      <div class="inline-flex items-center gap-2 px-3 py-1 bg-violet-100 dark:bg-violet-900/30 border border-violet-200 dark:border-violet-500/20 rounded-full text-sm text-violet-700 dark:text-violet-400 mb-6 font-semibold uppercase tracking-wider">
        <span>🔬</span>
        <span>Project-Based Learning</span>
      </div>
      <h1 class="text-4xl md:text-5xl font-extrabold mb-6 text-gray-900 dark:text-white tracking-tight">
        实战项目教程
      </h1>
      <p class="text-gray-600 dark:text-gray-400 text-lg max-w-2xl mx-auto leading-relaxed">
        通过真实项目学习，从零构建生产级 AI 应用
      </p>
    </div>

    <!-- Free Projects Section -->
    {freeProjects.length > 0 && (
      <section class="mb-16">
        <h2 class="text-2xl font-bold mb-6 flex items-center gap-3 text-gray-900 dark:text-gray-100">
          <span>🆓</span>
          <span>免费项目预览</span>
        </h2>
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
          {freeProjects.map(project => (
            <a href={getPath(`/vibecoding/${project.slug}`)} class="group block">
              <div class="bg-white dark:bg-[#1a1c23] border border-gray-200 dark:border-white/10 rounded-xl p-6 hover:border-blue-500/50 dark:hover:border-blue-400/50 transition-all duration-300 h-full relative overflow-hidden">
                <div class="absolute inset-0 bg-gradient-to-br from-blue-500/0 to-violet-500/0 group-hover:from-blue-500/5 group-hover:to-violet-500/5 transition-opacity"></div>
                <div class="relative z-10">
                  <div class="flex items-center gap-2 mb-3 flex-wrap">
                    <span class="px-2 py-1 bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-400 rounded text-xs font-semibold uppercase tracking-wider">
                      Free Preview
                    </span>
                    {project.data.level && (
                      <span class="px-2 py-1 bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-400 rounded text-xs font-semibold">
                        {project.data.level}
                      </span>
                    )}
                  </div>
                  <h3 class="font-bold mb-2 text-gray-900 dark:text-gray-100 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors">
                    {project.data.title}
                  </h3>
                  <p class="text-sm text-gray-600 dark:text-gray-400 line-clamp-3 leading-relaxed mb-4">
                    {project.data.description}
                  </p>
                  {project.data.githubRepo && (
                    <div class="flex items-center text-xs text-gray-500 dark:text-gray-400 mb-2">
                      <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 24 24">
                        <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                      </svg>
                      GitHub Repo
                    </div>
                  )}
                  {project.data.estimatedTime && (
                    <div class="text-xs text-gray-500 dark:text-gray-400 flex items-center gap-1">
                      ⏱️ {project.data.estimatedTime}
                    </div>
                  )}
                </div>
              </div>
            </a>
          ))}
        </div>
      </section>
    )}

    <!-- Premium Projects Section -->
    <section class="mb-16">
      <div class="flex items-center justify-between mb-6 flex-wrap gap-4">
        <h2 class="text-2xl font-bold flex items-center gap-3 text-gray-900 dark:text-gray-100">
          <span>🔒</span>
          <span>Premium 项目</span>
        </h2>
        <a
          href="https://xyzq.xyz"
          target="_blank"
          rel="noopener"
          class="text-blue-600 dark:text-blue-400 hover:text-blue-700 dark:hover:text-blue-300 font-semibold text-sm flex items-center gap-1 group"
        >
          <span>解锁所有教程</span>
          <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
          </svg>
        </a>
      </div>

      <div class="grid gap-8">
        {Object.entries(groupedPremium).map(([folder, items]) => (
          <div>
            <h3 class="text-lg font-bold mb-4 text-gray-800 dark:text-gray-200 flex items-center gap-2">
              <span>{folderNames[folder] || folder}</span>
              <span class="text-sm font-normal text-gray-500 dark:text-gray-400">({items.length})</span>
            </h3>
            <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
              {items.map(project => (
                <div class="relative group">
                  <!-- Lock overlay -->
                  <div class="absolute inset-0 bg-white/90 dark:bg-black/90 backdrop-blur-sm z-20 rounded-xl flex items-center justify-center p-6 transition-opacity">
                    <div class="text-center">
                      <div class="text-5xl mb-3">🔒</div>
                      <p class="font-bold text-gray-900 dark:text-gray-100 mb-1">Premium 内容</p>
                      <p class="text-sm text-gray-600 dark:text-gray-400 mb-4">加入知识星球解锁</p>
                      <a
                        href="https://xyzq.xyz"
                        target="_blank"
                        rel="noopener"
                        class="inline-block px-5 py-2.5 bg-gradient-to-r from-blue-600 to-violet-600 hover:from-blue-700 hover:to-violet-700 text-white rounded-lg font-bold transition-all duration-300 shadow-lg hover:shadow-xl text-sm"
                      >
                        立即解锁 (¥50/年)
                      </a>
                      <div class="mt-3 text-xs text-gray-500 dark:text-gray-400">
                        {project.data.estimatedTime && `⏱️ ${project.data.estimatedTime}`}
                      </div>
                    </div>
                  </div>

                  <!-- Project card (blurred) -->
                  <div class="bg-white dark:bg-[#1a1c23] border border-gray-200 dark:border-white/10 rounded-xl p-6 opacity-40 blur-[2px]">
                    <div class="flex items-center gap-2 mb-3">
                      <span class="px-2 py-1 bg-violet-100 dark:bg-violet-900/30 text-violet-700 dark:text-violet-400 rounded text-xs font-semibold uppercase tracking-wider">
                        Premium
                      </span>
                      <span class="px-2 py-1 bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-400 rounded text-xs font-semibold">
                        {project.data.level}
                      </span>
                    </div>
                    <h3 class="font-bold mb-2 text-gray-900 dark:text-gray-100">
                      {project.data.title}
                    </h3>
                    <p class="text-sm text-gray-600 dark:text-gray-400 line-clamp-3">
                      {project.data.description}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- Benefits Section -->
    <section class="bg-gradient-to-br from-blue-50 to-violet-50 dark:from-blue-900/20 dark:to-violet-900/20 border border-blue-200 dark:border-blue-500/20 rounded-2xl p-8 md:p-12">
      <h2 class="text-2xl md:text-3xl font-bold mb-8 text-center text-gray-900 dark:text-gray-100">
        会员权益
      </h2>
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6 mb-10">
        {[
          { icon: '📚', title: '完整教程', desc: '所有项目的详细实现步骤，从0到1手把手教学' },
          { icon: '🎥', title: '视频演示', desc: '高清视频 walkthrough，直观学习开发过程' },
          { icon: '💻', title: '源代码', desc: '完整项目源码访问，可直接运行和学习' },
          { icon: '💬', title: '社区支持', desc: '专属 Q&A 和代码审查，快速解决问题' },
        ].map(benefit => (
          <div class="text-center">
            <div class="text-4xl mb-3">{benefit.icon}</div>
            <h3 class="font-bold mb-2 text-gray-900 dark:text-gray-100">{benefit.title}</h3>
            <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">{benefit.desc}</p>
          </div>
        ))}
      </div>
      <div class="text-center">
        <a
          href="https://xyzq.xyz"
          target="_blank"
          rel="noopener"
          class="inline-flex items-center justify-center gap-2 px-10 py-4 bg-gradient-to-r from-blue-600 to-violet-600 hover:from-blue-700 hover:to-violet-700 text-white rounded-xl font-bold transition-all duration-300 shadow-lg hover:shadow-xl text-lg"
        >
          <span>加入知识星球</span>
          <span class="text-blue-100">（¥50/年）</span>
        </a>
        <p class="text-sm text-gray-600 dark:text-gray-400 mt-4 flex items-center justify-center gap-2">
          <svg class="w-4 h-4 text-green-500" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
          </svg>
          <span>已有 <span class="font-bold text-blue-600 dark:text-blue-400">{memberCount.toLocaleString()}+</span> 位开发者加入</span>
        </p>
      </div>
    </section>
  </div>
</Layout>
